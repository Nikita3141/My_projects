#include <iostream>
#include <stdio.h>

using namespace std;

int main() {
    int n, i = 0;
    int max1;
    while (cin >> n) {
        if (n == 0) break;
        else if (n>max1){
            max1=n;
            i++;
        }
    }
    cout << max;
}